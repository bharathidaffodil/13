#include <stdio.h>
 
void main()
{
    int num;
 
    
    if (num > 0)
        printf("%d Positive \n", num);
    else if (num < 0)
        printf("%d Negative \n", num);
    else
        printf("Zero");
}
